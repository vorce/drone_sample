pipeline:
  build:
    image: elixir:1.7-alpine
    commands:
      - apk add -qU git
      - mix local.hex --force
      - mix local.rebar --force
      - yes | mix deps.get
      - mix coveralls.drone