defmodule DroneSample do
  @moduledoc """
  Just some functions that we can add tests for to show the coverage.
  """

  def add(x, y) do
    x + y
  end

  def multiply(x, y) do
    x * y
  end

  def subtract(x, y) do
    x - y
  end
end
